# We bind only to localhost by default.
listener 1883 127.0.0.1

# Keep it simple for local dev.
allow_anonymous true

# Persist retained messages + state to disk in this workspace.
persistence true
persistence_location mosquitto-data/
autosave_interval 60

# Log to stdout
log_dest stdout

